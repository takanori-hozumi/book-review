<!--プロフィール-->
<!--ここでの修正を最小限にするため、呼び出し元でローカル変数を定義している-->
<h2 class="mt-3">User info</h2>
  <table class="table">
    <tr>
      <td><%= attachment_image_tag user,:profile_image, :fill, 60, 60, format:"jpeg",fallback:"no_image.jpg",size:"60x60" %></td>
      <td></td>
    </tr>
    <tr>
      <th>name</th>
      <th><%= user.name %>
    </tr>
    <tr>
      <th>introduction</th>
      <td><%= user.introduction %></td>
    </tr>
  </table>

  <%= link_to edit_user_path(current_user.id),class:"btn btn-block btn-outline-secondary p-0 mt-3" do %>
    <i class="fas fa-user-cog"></i>
  <% end %>